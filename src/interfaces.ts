interface Pokemon {
    // attack(): string;
    // gainXp(amount: number): void;
    // gainLife(amount: number): void
    // looseLife(amount: number): void;
    // isKo(): boolean;
    // lvlUp(): void;
}
